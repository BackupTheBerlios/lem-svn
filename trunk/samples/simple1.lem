// A simple example of LEM with just the bare framework 
//

model SimpleModel is 
  "a very simple example of LEM syntax"
  
  domain SimpleDomain is
    "a very simple domain"

    subsystem DefaultSubsystem is
        "a very simple subsystem"

    end DefaultSubsystem;

  end SimpleDomain;
end SimpleModel;
